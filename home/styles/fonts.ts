import { Ubuntu } from 'next/font/google';

//const ubuntu = Ubuntu({ subsets: ['latin'] });
const ubuntu400 = Ubuntu({ weight: '400', subsets: ['latin'] });
const ubuntuBold = Ubuntu({ weight: '700', subsets: ['latin'] });

export { ubuntu400, ubuntuBold };
